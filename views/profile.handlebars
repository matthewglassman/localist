<h1>Hello, {{dbusers.user_name}}!</h1>

<h3>Are you <a href="/seller">selling</a> or <a href="/buyer">buying</a> today?</h3>


	{{!-- <form id="userForm" action="/NEEDPATH" method="POST"> --}}
		

		{{#each students}}
			<p> {{this.systemNumber}} {{this.first_name}} {{this.last_name}}</p>

		{{/each}}
<h2> Add A New Profile</h2>
	<form class="create-update-form" action="/user" method="POST">
	<p>Enter New User Information</p>
	  <div class="form-group">
	  	<label for="ca">Email:</label>
	  	<input type="text" id="ca" name="systemNumber">
	  	<label for="ca">First Name:</label>
	  	<input type="text" id="ca" name="first_name">
	  	<label for="ca">Last Name:</label>
	  </div>
		<button class="submitButton"> Add New User </button>
	</form>

